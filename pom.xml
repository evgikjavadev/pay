<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>ru.vtb.msa.rfrm</groupId>
    <artifactId>rfrm-core</artifactId>
    <version>${revision}</version>
    <packaging>jar</packaging>

    <parent>
        <groupId>ru.vtb.msa.starter</groupId>
        <artifactId>parent</artifactId>
        <version>0.7.13</version>
    </parent>

    <properties>
        <revision>0.0.1</revision>
        <java.version>11</java.version>
        <finalName>example-integrations</finalName>
        <mapstruct.version>1.4.1.Final</mapstruct.version>
        <lombok.version>1.18.22</lombok.version>
        <lombok-mapstruct.version>0.2.0</lombok-mapstruct.version>
        <omni-jwt-common-lib-servlet.version>30.0.0.1</omni-jwt-common-lib-servlet.version>
        <omni-jwt-common-lib.version>20.3.0.2</omni-jwt-common-lib.version>
        <omni-jwt-security-lib-starter.version>20.3.0.2</omni-jwt-security-lib-starter.version>
        <omni-access-control-lib.version>20.3.0.7</omni-access-control-lib.version>
        <audit.version>20.3.0.8</audit.version>
        <springdoc.version>1.5.6</springdoc.version>
        <logbook.version>2.15.0</logbook.version>
        <tstr-starter.version>4.2.1.9</tstr-starter.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.prometheus</groupId>
            <artifactId>simpleclient</artifactId>
        </dependency>

        <!--Логирование запроссов и ответов -->
        <dependency>
            <groupId>ru.vtb.msa.starter</groupId>
            <artifactId>mdc-logging-httpserver-servlet</artifactId>
        </dependency>
        <dependency>
            <groupId>ru.vtb.msa.starter</groupId>
            <artifactId>mdc-logging-client-webclient</artifactId>
        </dependency>

        <!-- Аудит -->
        <!-- модуль хранения событий аудита в оперативной памяти -->
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>audit-lib-in-memory-storage</artifactId>
            <version>${audit.version}</version>
        </dependency>
        <!-- модуль отправки событий аудита непосредственно в кафку -->
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>audit-lib-kafka-sender</artifactId>
            <version>${audit.version}</version>
        </dependency>
        <!-- модуль шаблонизатора событий аудита - freemarker -->
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>audit-lib-freemarker-template-resolver</artifactId>
            <version>${audit.version}</version>
        </dependency>
        <!-- модуль контекста из которого используется для получения инициатора запроса, в даном случае servlet context -->
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>audit-lib-servlet-context</artifactId>
            <version>${audit.version}</version>
        </dependency>

        <!-- Журналирование логов -->
        <dependency>
            <groupId>ru.vtb.infra.logging</groupId>
            <artifactId>logback-integration</artifactId>
            <version>1.4.33</version>
            <scope>compile</scope>
        </dependency>

        <!-- Трассировка -->
        <dependency>
            <groupId>ru.vtb.tstr</groupId>
            <artifactId>tstr-starter</artifactId>
            <version>${tstr-starter.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>ru.vtb.tsds</groupId>
                    <artifactId>tsds-starter</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>

        <!-- spring boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security.oauth</groupId>
            <artifactId>spring-security-oauth2</artifactId>
            <version>2.5.2.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-jose</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>omni-jwt-common-lib</artifactId>
            <version>${omni-jwt-common-lib.version}</version>
        </dependency>
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>omni-jwt-common-servlet</artifactId>
            <version>${omni-jwt-common-lib-servlet.version}</version>
        </dependency>
        <!-- TSRM -->
        <!-- модуль библиотеки ролевой -->
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>omni-access-control-lib-servlet</artifactId>
            <version>${omni-access-control-lib.version}</version>
        </dependency>
        <!-- стартер jwt security -->
        <dependency>
            <groupId>ru.vtb.omni</groupId>
            <artifactId>omni-jwt-security-servlet-starter</artifactId>
            <version>${omni-jwt-security-lib-starter.version}</version>
        </dependency>
        <!-- Spring Kafka scoped as provided in omni-access-control-lib -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>

        <dependency>
            <groupId>org.zalando</groupId>
            <artifactId>logbook-bom</artifactId>
            <version>${logbook.version}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
    <build>
        <finalName>${finalName}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.22.2</version>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <version>1.2.5</version>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>resolveCiFriendliesOnly</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>2.6.3</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <environmentVariables>
                        <revision>${revision}</revision>
                    </environmentVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <!--  <version>3.8.1</version>-->
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.22</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>${lombok-mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>